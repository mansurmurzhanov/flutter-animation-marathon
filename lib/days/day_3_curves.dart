import 'package:flutter/material.dart';

class Day3CurvesPage extends StatelessWidget {
  const Day3CurvesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Day 3 - Tween + Curves'),
      ),
    );
  }
}